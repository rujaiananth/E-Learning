apiVersion: apps/v1
kind: Deployment
metadata:
  name: elearning-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: elearning
  template:
    metadata:
      labels:
        app: elearning
    spec:
      containers:
      - name: elearning
        image: rujairj/elearning-website:v1
        ports:
        - containerPort: 80
